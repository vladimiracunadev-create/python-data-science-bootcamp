# 📝 Tarea — Clase 16
> 📝 Trabajo autónomo para consolidar lo visto y practicar con más calma.

## 🎯 Encargo

Usando el dataset `ventas_tienda.csv` (o un dataset propio si ya tienes uno), realiza una exploración visual completa con Seaborn. Debes crear al menos 5 gráficos distintos (cada uno de un tipo diferente) y para cada uno escribir:

1. **La pregunta que querías responder** con ese gráfico (como comentario en el código).
2. **Una conclusión de 2-3 líneas** basada en lo que observaste (como comentario al final del bloque o en una celda de markdown si usas notebook).

Los tipos de gráfico requeridos son:
- Un `histplot` o `kdeplot` de una variable numérica
- Un `boxplot` o `violinplot` comparando categorías
- Un `barplot` con una variable categórica
- Un `scatterplot` con `hue` o `size`
- Un `heatmap` de correlación

Como gráfico opcional (punto extra): `pairplot` sobre el dataset `estudiantes.csv`.

## 📦 Entregables
- Código o desarrollo ordenado.
- Conclusión breve conectada con evidencia.
- Comentarios que expliquen qué hace y para qué sirve cada bloque importante.

## 🔍 Autoevaluación final
- Entendí la pregunta del módulo.
- Puedo explicar la salida sin leer el código completo.
- Dejé comentarios útiles en los pasos clave.
