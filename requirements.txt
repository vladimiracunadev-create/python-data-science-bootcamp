# Python Data Science Program — requirements completos del curso
#
# Filosofía: bundle único para que el alumno haga `pip install -r requirements.txt`
# UNA vez y todos los notebooks del currículo (232 clases · 9 partes) ejecuten.
# Tamaño aproximado: ~2.5 GB con torch CPU. Si necesitás un subset liviano para
# CI o smoke tests, instalá solo la sección "App web" + "Núcleo data science".
#
# Compatibilidad: probado en Windows 11 + Python 3.12. Linux/macOS también
# soportados; macOS Apple Silicon requiere arch arm64 nativo para torch.

# ─── App de escritorio Windows nativa (Qt) ───────────────────────────────────
# Viewer del currículo en widgets Qt puros (sin WebView, sin Flask, sin localhost).
PySide6>=6.6

# ─── App web del laboratorio (Flask + Jupyter kernel) ────────────────────────
flask>=3.0
markdown>=3.6
jupyter_client>=8.6
ipykernel>=6.29
nbformat>=5.10
jupyter>=1.0
notebook>=7.0

# ─── Núcleo data science (todas las partes) ──────────────────────────────────
numpy>=1.26
pandas>=2.2
polars>=1.0
pyarrow>=15.0
scipy>=1.12
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20
pillow>=10.0

# ─── Machine Learning clásico (Parte 1) ──────────────────────────────────────
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.3
catboost>=1.2
joblib>=1.3
imbalanced-learn>=0.12
shap>=0.45

# ─── Deep Learning (Parte 2) ─────────────────────────────────────────────────
# Torch CPU por default; el alumno con GPU instala torch+cu* aparte.
torch>=2.2
torchvision>=0.17
torchaudio>=2.2
lightning>=2.2
transformers>=4.40
sentence-transformers>=2.7
datasets>=2.18
accelerate>=0.29
peft>=0.10
timm>=1.0
albumentations>=1.4
opencv-python-headless>=4.9

# ─── Estadística inferencial y causal (Parte 3) ──────────────────────────────
statsmodels>=0.14
pymc>=5.10
arviz>=0.17
lifelines>=0.27

# ─── MLOps (Parte 4) ─────────────────────────────────────────────────────────
mlflow>=2.12
optuna>=3.6
evidently>=0.4
great-expectations>=0.18
pandera>=0.18
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6

# ─── Ingeniería de datos (Parte 5) ───────────────────────────────────────────
duckdb>=0.10
sqlalchemy>=2.0
# Airflow y PySpark se mencionan en el currículo pero son pesados y específicos
# de plataforma; el alumno los instala aparte si los necesita.
# apache-airflow>=2.9
# pyspark>=3.5

# ─── Sistemas de recomendación (Parte 6) ─────────────────────────────────────
implicit>=0.7
faiss-cpu>=1.8
# `scikit-surprise` y `lightfm` requieren toolchain de compilación en Windows.
# Documentados en clase 222; opt-in con conda si los necesitás.
# scikit-surprise>=1.1
# lightfm>=1.17

# ─── Ética, fairness, privacidad (Parte 7) ───────────────────────────────────
fairlearn>=0.10
diffprivlib>=0.6
# aif360 y opacus tienen deps GPU/compilación; opt-in en clases 224 y 225.
# aif360>=0.6
# opacus>=1.4

# ─── Visualización y reporting ───────────────────────────────────────────────
streamlit>=1.32

# ─── Generación de PDF (utilidades del repo) ─────────────────────────────────
fpdf2>=2.7

# ─── Desarrollo y testing ────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
