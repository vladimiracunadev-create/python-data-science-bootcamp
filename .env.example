# Python Data Science Program — local runtime
PROGRAM_HOST=127.0.0.1
PROGRAM_PORT=8000

# Runtime notes
# - Mantener 127.0.0.1 cuando el uso sea local.
# - Si se expone detras de un proxy, agregar autenticacion, TLS y rate limiting fuera de esta app.
